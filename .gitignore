# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Supabase CLI local state
supabase/.temp/

# Claude Code local settings (per-machine)
.claude/settings.local.json

# Playwright
test-results/
playwright-report/
# Capturi si jurnale lasate de verificarile prin MCP
.playwright-mcp/

# Raportul de audit, generat local (binar, publicat si ca artifact)
La Livada PMS - Audit.pdf

# Build-uri
dist-booking/
dist-guest/

# Backup-uri: contin date personale ale clientilor
backup-*.sql
.refactor/
.refactor/

# Instantanee de lucru din refactorizari vechi. Cod mort, 265 KB bucata,
# pastrate local daca mai trebuie privite. Regula acoperea doar `backup-*.sql`,
# deci cele trei .jsx erau urmarite fara sa vrea nimeni.
backup-*

# Pozele brute de pe telefon, inainte de scripts/acces-poze.mjs. Sunt de
# 3-4 MB bucata si nu au ce cauta in istoric — in repo intra doar cele
# procesate, din public-guest/acces/.
poze-acces/
