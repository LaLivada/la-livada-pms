# Traducerea guest app-ului în 7 limbi — Plan de implementare

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Pagina oaspetelui (`src/guest/`) se deschide azi doar în română.
Planul ăsta o face să pornească automat în limba telefonului (RO, EN, FR,
IT, DE, RU, UK), cu un selector manual cu steaguri emoji, fără să crească
bundle-ul de șapte ori pentru text pe care majoritatea oaspeților nu-l
citesc niciodată.

**Architecture:** Conținutul static (`continut.js`) se împarte în date
language-independent (`date.js`) și conținut editorial per limbă
(`continut.<lang>.js` + `interfata.<lang>.js`, aceeași formă în toate).
Stratul mic (interfață, Bun venit, Important) e static importat, ales la
randare printr-un React Context. Stratul mare (Regulament, Atracții) se
încarcă cu `import()` dinamic, doar pentru limba curentă, doar când
oaspetele deschide panoul.

**Tech Stack:** React 19 (guest app-ul folosește deja hooks — `useState`,
`useContext`, `useEffect`), Vite (code-splitting automat la `import()`
dinamic), Vitest.

**Spec:** [docs/guest-app-i18n.md](../../guest-app-i18n.md) — citește-l
înainte de Task 1. Deciziile din §0 (limbi, fallback engleză, steaguri
emoji, persistență localStorage) nu se redeschid aici.

## Global Constraints

- Șapte limbi, coduri ISO exacte: `ro`, `en`, `fr`, `it`, `de`, `ru`, `uk`
  (nu `ua`).
- Fallback pentru o limbă nerecunoscută: `en`.
- Steaguri: emoji, nu SVG (`🇷🇴 🇬🇧 🇫🇷 🇮🇹 🇩🇪 🇷🇺 🇺🇦`).
- Alegerea manuală se ține minte în `localStorage`, cheia `g-limba`, și
  câștigă față de detectarea automată.
- Traducerile reale (EN/FR/IT/DE/RU/UK) se redactează de către executor
  și se arată proprietarului, limbă cu limbă, într-un mesaj din chat,
  ÎNAINTE de commit — niciodată commise fără aprobare explicită.
- Numele rețelei Wi-Fi, numerele de telefon, coordonatele, adresele și
  numele proprii NU se traduc (rămân identice în toate cele 7 fișiere).
- `npm run build:guest` trebuie să rămână curat (fără erori) și
  `npx vitest run` trebuie să treacă integral la finalul fiecărui task.

---

## File Structure

```
src/guest/
  date.js                 (NOU) — fapte operaționale, aceleași în toate limbile
  continut.js              (MODIFICAT) — dispecer subțire, alege modulul potrivit
  continut.ro.js           (NOU, mutat din continut.js) — conținut editorial RO
  continut.en.js           (NOU) — idem, engleză
  continut.fr.js           (NOU) — idem, franceză
  continut.it.js           (NOU) — idem, italiană
  continut.de.js           (NOU) — idem, germană
  continut.ru.js           (NOU) — idem, rusă
  continut.uk.js           (NOU) — idem, ucraineană
  continut-mare.js         (NOU) — dispecer pentru Regulament + Atracții (import() dinamic)
  interfata.ro.js          (NOU) — texte de interfață (butoane, etichete, erori), RO
  interfata.en.js / .fr.js / .it.js / .de.js / .ru.js / .uk.js  (NOU) — idem
  interfata.js              (NOU) — dispecer subțire pentru interfata.<lang>.js
  limbi.jsx                  (NOU) — catalogul limbilor, detectare, Context, localStorage
  continut-forme.test.js    (NOU) — testul de formă comună între cele 7 limbi
  limbi.test.js             (NOU) — teste pentru detectare/persistare
  App.jsx                   (MODIFICAT) — importă din interfata.js/continut.js, selector nou
  Fisa.jsx                  (MODIFICAT) — importă etichete/erori din interfata.js
  Fereastra.jsx             (NESCHIMBAT) — reutilizată de popup-ul selectorului
guest/
  main.jsx                  (MODIFICAT) — înveleşte <App/> cu <LimbaProvider>
```

**Interfaces (contractul între fișiere, valabil pentru tot planul):**

```js
// limbi.jsx
export const LIMBI = [
  { cod: "ro", nume: "Română",      steag: "🇷🇴" },
  { cod: "en", nume: "English",     steag: "🇬🇧" },
  { cod: "fr", nume: "Français",    steag: "🇫🇷" },
  { cod: "it", nume: "Italiano",    steag: "🇮🇹" },
  { cod: "de", nume: "Deutsch",     steag: "🇩🇪" },
  { cod: "ru", nume: "Русский",     steag: "🇷🇺" },
  { cod: "uk", nume: "Українська",  steag: "🇺🇦" },
];
export const LIMBA_IMPLICITA = "en";
export function detecteazaLimba(navigatorLanguages) // string[] -> cod (unul din LIMBI, sau LIMBA_IMPLICITA)
export function LimbaProvider({ children })
export function useLimba() // -> { cod, steag, seteazaLimba(cod) }

// continut.js (dispecer, exporturi neschimbate fata de azi minus REGULAMENT/ATRACTII)
export const TELEFON, TELEFON_SCRIS, FIRMA, ASISTENTA, ACASA, LINK_MAPS,
  LINK_WAZE, HARTA_INCORPORATA, ACCES_CAMERE, WIFI, ATRACTII_PE_PAGINA,
  linkHarta; // re-exportate direct din date.js, neschimbate de limba curenta
export function useContinutMic() // -> { BUN_VENIT, IMPORTANT } pentru limba curenta

// continut-mare.js
export function useRegulament() // -> { stare: "incarca"|"gata", regulament }
export function useAtractiiTextMare() // -> { stare: "incarca"|"gata", texte } (merge cu date.js prin `cheie`)

// interfata.js
export function useTexte() // -> obiectul TEXTE pentru limba curenta (vezi Task 3)
```

---

### Task 1: Împarte `continut.js` în `date.js` (fapte) și `continut.ro.js` (editorial)

**Files:**
- Create: `src/guest/date.js`
- Create: `src/guest/continut.ro.js`
- Modify: `src/guest/continut.js` (devine re-export din cele două, temporar — dispecerul real vine în Task 4)
- Modify: `src/guest/App.jsx` (niciun import nu se schimbă — `continut.js` păstrează aceleași nume exportate)
- Test: `src/guest-stiluri.test.js` și restul suitei existente trebuie să treacă neschimbate (nimic din CSS/comportament nu se atinge în task-ul ăsta)

**Interfaces:**
- Produce: `date.js` exportă `TELEFON, TELEFON_SCRIS, FIRMA, ASISTENTA` (fără câmpul `raspuns`), `ACASA, LINK_MAPS, LINK_WAZE, HARTA_INCORPORATA, ACCES_CAMERE` (cu `poze` redus la `{ fisier }`, fără `descriere`), `WIFI, ATRACTII` (câmpurile factuale: `cheie, loc, km, minute, foto, cauta, credit` — FĂRĂ `nume`, FĂRĂ `text`), `ATRACTII_PE_PAGINA, linkHarta`.
- Produce: `continut.ro.js` exportă `asistentaRaspuns` (string), `BUN_VENIT` (cu `tare` scos din obiectul de date — vezi mai jos), `IMPORTANT`, `REGULAMENT`, `ACCES_CAMERE_DESCRIERI` (array de `{ fisier, descriere }`), `ATRACTII_TEXT` (array de `{ cheie, nume, text }`).

- [ ] **Step 1: Creează `src/guest/date.js`**

```js
/* Faptele operationale ale complexului — coordonate, numere de telefon,
 * numele retelei Wi-Fi, ordinea pozelor de acces, datele factuale ale
 * atractiilor (distanta, poza, sursa). NU se traduc si NU se dubleaza
 * per limba: un singur fisier, indiferent cate limbi are pagina.
 *
 * Contine EXACT ce ramane identic in toate cele 7 limbi. Textul care se
 * traduce (descrieri, introduceri, reguli) sta in continut.<lang>.js. */

export const TELEFON = "+40722899899";
export const TELEFON_SCRIS = "+40 722 899 899";
export const FIRMA = "S.C. OVISER S.R.L. · CUI RO33918057";

/* `nume`, `telefon`, `scris`, `wa` nu se traduc — un nume de persoana si
 * niste cifre. Propozitia „raspunde in cateva minute" e text, deci sta
 * in continut.<lang>.js, ca `asistentaRaspuns`. */
export const ASISTENTA = {
  nume: "Răzvan",
  telefon: "+40725259999",
  scris: "+40 725 259 999",
  wa: "40725259999",
};

export const ACASA = { lat: 46.6225253, lon: 27.7551750, localitate: "Vaslui" };

const ADRESA = "Complex La Livada, DN24, nr. 743, Muntenii de Jos, jud. Vaslui";
export const LINK_MAPS =
  `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(ADRESA)}`;
export const LINK_WAZE =
  `https://www.waze.com/ul?q=${encodeURIComponent(ADRESA)}&navigate=yes`;
export const HARTA_INCORPORATA =
  `https://maps.google.com/maps?q=${ACASA.lat},${ACASA.lon}` +
  `&t=h&z=16&hl=ro&ie=UTF8&output=embed`;

/* `poze` tine DOAR ordinea si numele fisierului — descrierea (text) e in
 * continut.<lang>.js, legata prin `fisier`, nu prin pozitie in array: o
 * traducere care schimba ordinea din greseala nu strica potrivirea. */
export const ACCES_CAMERE = {
  poze: [
    { fisier: "1-intrarea.jpg" },
    { fisier: "2-aleea.jpg" },
    { fisier: "3-parcarea.jpg" },
    { fisier: "4-poteca.jpg" },
  ],
  pasi: [],
};

export const WIFI = { retea: "La Livada WiFi" };

/* Campurile factuale ale atractiilor, legate de text prin `cheie` — vezi
 * continut.<lang>.js pentru `nume`/`text`. */
export const ATRACTII = [
  { cheie: "curtea", loc: "Vaslui", km: 3, minute: 4,
    cauta: "Curtea Domnească Vaslui", foto: "curtea.jpg",
    credit: { autor: "Adrian Baciu", licenta: "CC BY-SA 3.0 RO",
      pagina: "https://commons.wikimedia.org/wiki/File:Biserica_Taierea_Capului_Sfantului_Ioan_Botezatorul_0082.jpg" } },
  { cheie: "muzeuvs", loc: "Vaslui", km: 3, minute: 5,
    cauta: "Muzeul Județean Ștefan cel Mare Vaslui", foto: "muzeuvs.jpg" },
  { cheie: "copou", loc: "Vaslui", km: 4, minute: 7,
    cauta: "Parcul Copou Vaslui", foto: "copou.jpg",
    credit: { autor: "Gabysor", licenta: "CC BY-SA 4.0",
      pagina: "https://commons.wikimedia.org/wiki/File:Parcul_Copou.jpg" } },
  { cheie: "statuie", loc: "Băcăoani, com. Muntenii de Jos", km: 6, minute: 10,
    cauta: "Statuia ecvestră Ștefan cel Mare Podul Înalt Băcăoani", foto: "statuie.jpg" },
  { cheie: "dinoparc", loc: "Secuia, com. Muntenii de Jos", km: 8, minute: 13,
    cauta: "Dino Parc Moldova Secuia Vaslui", foto: "dino.jpg" },
  { cheie: "solesti", loc: "Solești", km: 21, minute: 31,
    cauta: "Conacul Rosetti-Solescu Solești Vaslui", foto: "solesti.jpg",
    credit: { autor: "Cezar Suceveanu", licenta: "CC BY 2.5",
      pagina: "https://commons.wikimedia.org/wiki/File:Conacul_din_Sole%C5%9Fti.jpg" } },
  { cheie: "burcel", loc: "Miclești", km: 28, minute: 38,
    cauta: "Movila lui Burcel Miclești Vaslui", foto: "burcel.jpg",
    credit: { autor: "FoamBubbles", licenta: "CC BY-SA 4.0",
      pagina: "https://commons.wikimedia.org/wiki/File:Situl_istoric_%E2%80%9EMovila_lui_Burcel%E2%80%9D_monument.jpg" } },
  { cheie: "racovita", loc: "com. Emil Racoviță", km: 34, minute: 38,
    cauta: "Casa memorială Emil Racoviță Vaslui", foto: "racovita.jpg",
    credit: { autor: "Cezar Suceveanu", licenta: "CC BY 2.5",
      pagina: "https://commons.wikimedia.org/wiki/File:Casa_memorial%C4%83_Emil_Racovi%C8%9B%C4%83.jpg" } },
  { cheie: "zoo", loc: "Bârlad", km: 48, minute: 50,
    cauta: "Grădina Zoologică Bârlad", foto: "zoo.jpg" },
  { cheie: "parvan", loc: "Bârlad", km: 50, minute: 52,
    cauta: "Muzeul Vasile Pârvan Bârlad", foto: "parvan.jpg",
    credit: { autor: "ElaG", licenta: "CC BY-SA 3.0 RO",
      pagina: "https://commons.wikimedia.org/wiki/File:Muzeul_%22Vasile_Parvan%22_Barlad.jpg" } },
];

export const ATRACTII_PE_PAGINA = 5;

export const linkHarta = (a) =>
  `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(a.cauta)}`;
```

- [ ] **Step 2: Creează `src/guest/continut.ro.js`**

Mută din `continut.js` actual: `BUN_VENIT`, `IMPORTANT`, `REGULAMENT`
(toate cele 25 de puncte, EXACT cum sunt azi — text juridic, nu se
atinge), plus adaugă `asistentaRaspuns`, `ACCES_CAMERE_DESCRIERI` și
`ATRACTII_TEXT` extrase din structurile vechi:

```js
/* Continutul editorial in romana — sursa pentru celelalte 6 limbi.
 * Aceeasi forma (aceleasi chei) trebuie sa existe si in continut.en.js,
 * continut.fr.js, continut.it.js, continut.de.js, continut.ru.js,
 * continut.uk.js — verificat de continut-forme.test.js. */

export const asistentaRaspuns = "răspunde în câteva minute";

export const BUN_VENIT = {
  intro: "Ne bucurăm că ești aici și îți dorim o ședere plăcută.",
  puncte: [
    {
      titlu: "Wi-Fi gratuit",
      inainte: "Rețeaua ",
      dupa: ", fără parolă.",
      actiune: "wifi",
    },
    {
      titlu: "Salvează pagina pe telefon",
      text: "Codul și butonul de deschidere rămân la îndemână tot sejurul.",
      actiune: "instalare",
    },
  ],
};

export const IMPORTANT = [
  { titlu: "Fumatul", text: "Interzis în cameră și în spațiile interioare — afară se poate. Fumatul în cameră se taxează cu 500 de lei." },
  { titlu: "Liniștea", text: "Între 22:00 și 08:00 te rugăm să păstrezi liniștea." },
  { titlu: "Animale de companie", text: "Sunt binevenite, cu o taxă de 50 de lei pe sejur." },
  { titlu: "Parcarea", text: "Gratuită în incintă, pe răspunderea proprietarului mașinii." },
];

export const REGULAMENT = {
  intro: "Oaspeții au luat la cunoștință următoarele reguli și se obligă să le respecte:",
  reguli: [
    // ... EXACT cele 25 de linii din src/guest/continut.js de azi (liniile
    // 207-233), copiate cuvant cu cuvant, fara nicio schimbare.
  ],
};

export const ACCES_CAMERE_DESCRIERI = [
  { fisier: "1-intrarea.jpg", descriere: "Intrați prin parcarea din dreapta și urmați sensul de mers." },
  { fisier: "2-aleea.jpg",    descriere: "La acest panou continuați să mergeți drept înainte." },
  { fisier: "3-parcarea.jpg", descriere: "Ați ajuns în parcare." },
  { fisier: "4-poteca.jpg",   descriere: "Accesul spre camere se face pe aleea din dreapta, prin fața Grand'Or Ballroom." },
];

export const ATRACTII_TEXT = [
  { cheie: "curtea", nume: "Curtea Domnească", text: "Ruinele curții ridicate de Alexandru cel Bun și extinse de Ștefan cel Mare între 1472 și 1490, lângă biserica „Tăierea Capului Sf. Ioan Botezătorul”, ctitorie a lui Ștefan din 1490." },
  { cheie: "muzeuvs", nume: "Muzeul Județean „Ștefan cel Mare”", text: "Colecția de bază a județului: istorie, arheologie, etnografie, artă populară și artă modernă, din antichitate până azi." },
  { cheie: "copou", nume: "Parcul Copou", text: "Cea mai mare grădină publică a orașului. Pe Aleea Scriitorilor sunt busturile lui Eminescu, Creangă, Kogălniceanu, Alecu Russo și Costache Negri." },
  { cheie: "statuie", nume: "Statuia ecvestră a lui Ștefan cel Mare", text: "Călărețul de bronz de 6,90 m, ridicat în 1975 pe un soclu de 8 m, la 500 de ani de la bătălia de la Podul Înalt din 10 ianuarie 1475. Sculptor: Mircea Ștefănescu. Se urcă pe 27 de trepte, iar la bază arde o flacără veșnică." },
  { cheie: "dinoparc", nume: "Dino Parc Moldova", text: "Peste 30 de dinozauri în mărime naturală, pe 2,2 hectare la marginea pădurii Crasna, cu poteci de peste 1,5 km, punți suspendate și un tunel. Deschis în 2026. Verifică programul pe Google Maps înainte să pornești." },
  { cheie: "solesti", nume: "Conacul Rosetti-Solescu", text: "Casa copilăriei Elenei Cuza, ridicată pe la 1827. Conacul e în ruină și nu se vizitează pe dinăuntru — de văzut rămân parcul cu arbori seculari, biserica „Adormirea Maicii Domnului” și mormântul Elenei Cuza din curtea ei." },
  { cheie: "burcel", nume: "Movila lui Burcel", text: "Monument și mănăstire pe o movilă cu priveliște largă peste dealurile Vasluiului. Legenda spune că aici l-ar fi ajutat Ștefan cel Mare la arat pe răzeșul Burcel." },
  { cheie: "racovita", nume: "Casa memorială „Emil Racoviță”", text: "Muzeu dedicat savantului, cu fotografii, documente de familie și obiecte din expediția antarctică a navei Belgica." },
  { cheie: "zoo", nume: "Grădina Zoologică", text: "Șase hectare de pădure veche la marginea orașului, cu zeci de specii de mamifere, păsări și pești, printre care lei și tigri. Deschisă din 1959." },
  { cheie: "parvan", nume: "Muzeul „Vasile Pârvan”", text: "Cel mai mare muzeu al județului, cu cinci secții — arheologie, artă, științele naturii, astronomie și oameni ai Bârladului — și singurul planetariu digital din Vaslui, deschis în 2009." },
];
```

Notă pentru executor: copiază cele 25 de linii din `REGULAMENT.reguli` (azi
în `src/guest/continut.js:207-233`) EXACT, caracter cu caracter — nu le
retranscrie din memorie.

- [ ] **Step 3: Fă din `continut.js` un re-export temporar (single-limbă)**

```js
/* Dispecer temporar — pana la Task 4, care il face sa aleaga dupa limba
 * curenta. Pana atunci, pagina ramane in romana, exact ca azi. */
export * from "./date.js";
export {
  BUN_VENIT, IMPORTANT, REGULAMENT,
} from "./continut.ro.js";
```

(Notă: `ACCES_CAMERE_DESCRIERI`, `ATRACTII_TEXT`, `asistentaRaspuns` NU
se re-exportă încă — nimic din `App.jsx`/`Fisa.jsx` nu le cere sub numele
ăsta până la Task 3, care le conectează prin `interfata.js`.)

- [ ] **Step 4: Actualizează cele patru locuri care foloseau `ASISTENTA.raspuns` și `ACCES_CAMERE.poze[i].descriere`**

`App.jsx:1154` (`<b>{ASISTENTA.nume}</b> — {ASISTENTA.raspuns}`) devine
`<b>{ASISTENTA.nume}</b> — {asistentaRaspuns}`, cu un nou import:
`import { asistentaRaspuns } from "./continut.ro.js";` (temporar — Task 4
îl mută pe `useAsistentaRaspuns()`, definit în `continut.js`).

Componenta `ContinutAcces` (căutată cu `Grep -n "poze\[" src/guest/App.jsx`)
combină `ACCES_CAMERE.poze` (din `date.js`, ordinea + fișierul) cu
`ACCES_CAMERE_DESCRIERI` (din `continut.ro.js`, descrierea) prin
`fisier`:

```js
import { ACCES_CAMERE_DESCRIERI } from "./continut.ro.js"; // temporar

const descrierePentru = (fisier) =>
  ACCES_CAMERE_DESCRIERI.find((d) => d.fisier === fisier)?.descriere || "";
```

Folosește `descrierePentru(poza.fisier)` oriunde codul citea azi
`poza.descriere`.

Panoul de atracții (căutat cu `Grep -n "ATRACTII\." src/guest/App.jsx`)
combină `ATRACTII` (din `date.js`) cu `ATRACTII_TEXT` (din `continut.ro.js`)
prin `cheie`, similar.

- [ ] **Step 5: Rulează suita completă**

Run: `npx vitest run`
Expected: toate testele existente trec, neschimbate — task-ul ăsta doar
mută cod, nu schimbă comportament.

- [ ] **Step 6: Verifică vizual guest app-ul**

Run: `npm run dev:guest`, deschide o rezervare de test, confirmă că
pagina arată identic cu înainte de task (hero, fișă, cele patru
scurtături, „Cum ajungi la noi", acces, regulament, atracții).

- [ ] **Step 7: Commit**

```bash
git add src/guest/date.js src/guest/continut.ro.js src/guest/continut.js src/guest/App.jsx
git commit -m "Guest app: continutul static impartit in date.js (fapte) si continut.ro.js (editorial)"
```

---

### Task 2: Catalogul limbilor, detectare, Context, persistare

**Files:**
- Create: `src/guest/limbi.jsx`
- Create: `src/guest/limbi.test.js`
- Modify: `guest/main.jsx` (înveleşte `<App/>` cu `<LimbaProvider>`)

**Interfaces:**
- Consumes: nimic din task-urile anterioare.
- Produces: `LIMBI`, `LIMBA_IMPLICITA`, `detecteazaLimba(navigatorLanguages)`,
  `LimbaProvider`, `useLimba()` — vezi contractul din §File Structure.
  Task 3 și Task 4 depind de `useLimba().cod`.

- [ ] **Step 1: Scrie testele pentru `detecteazaLimba`**

```js
// src/guest/limbi.test.js
import { describe, it, expect } from "vitest";
import { detecteazaLimba, LIMBI, LIMBA_IMPLICITA } from "./limbi.jsx";

describe("detecteazaLimba", () => {
  it("recunoaste codul exact", () => {
    expect(detecteazaLimba(["ro"])).toBe("ro");
    expect(detecteazaLimba(["de"])).toBe("de");
  });

  it("ignora regiunea din eticheta BCP-47", () => {
    expect(detecteazaLimba(["en-US"])).toBe("en");
    expect(detecteazaLimba(["en-GB"])).toBe("en");
    expect(detecteazaLimba(["ro-RO"])).toBe("ro");
  });

  it("ia prima limba recunoscuta din lista, in ordine", () => {
    // Spaniola nu e suportata, a doua e germana.
    expect(detecteazaLimba(["es-ES", "de-DE"])).toBe("de");
  });

  it("da limba implicita cand nimic nu se potriveste", () => {
    expect(detecteazaLimba(["es-ES", "ja-JP"])).toBe(LIMBA_IMPLICITA);
  });

  it("da limba implicita pentru lista goala sau lipsa", () => {
    expect(detecteazaLimba([])).toBe(LIMBA_IMPLICITA);
    expect(detecteazaLimba(undefined)).toBe(LIMBA_IMPLICITA);
  });

  it("fiecare limba din LIMBI e formata din cod, nume si steag", () => {
    expect(LIMBI.length).toBe(7);
    for (const l of LIMBI) {
      expect(l.cod).toMatch(/^[a-z]{2}$/);
      expect(l.nume.length).toBeGreaterThan(0);
      expect(l.steag.length).toBeGreaterThan(0);
    }
  });
});
```

- [ ] **Step 2: Rulează testul, verifică eșecul**

Run: `npx vitest run src/guest/limbi.test.js`
Expected: FAIL — `limbi.jsx` nu există încă.

- [ ] **Step 3: Scrie `src/guest/limbi.jsx`**

```js
/* Limbile paginii oaspetelui — catalogul, detectarea dupa telefon si
 * persistarea alegerii manuale.
 *
 * Coduri ISO 639-1, nu ad-hoc: `uk` pentru ucraineana, nu `ua` (`UA` e
 * codul de TARA, nu de limba — o confuzie frecventa).
 *
 * Engleza, nu romana, e limba de rezerva: un turist a carui limba nu e
 * in lista are sanse mai mari sa inteleaga engleza decat romana. */
import { createContext, useContext, useEffect, useMemo, useState } from "react";

export const LIMBI = [
  { cod: "ro", nume: "Română",     steag: "🇷🇴" },
  { cod: "en", nume: "English",    steag: "🇬🇧" },
  { cod: "fr", nume: "Français",   steag: "🇫🇷" },
  { cod: "it", nume: "Italiano",   steag: "🇮🇹" },
  { cod: "de", nume: "Deutsch",    steag: "🇩🇪" },
  { cod: "ru", nume: "Русский",    steag: "🇷🇺" },
  { cod: "uk", nume: "Українська", steag: "🇺🇦" },
];

export const LIMBA_IMPLICITA = "en";

const CODURI = new Set(LIMBI.map((l) => l.cod));

/* Primele doua litere dintr-o eticheta BCP-47 ("en-US" -> "en"), fara sa
 * presupunem ca eticheta are neaparat o regiune. */
function codDeLimba(eticheta) {
  return String(eticheta || "").slice(0, 2).toLowerCase();
}

export function detecteazaLimba(navigatorLanguages) {
  for (const eticheta of navigatorLanguages || []) {
    const cod = codDeLimba(eticheta);
    if (CODURI.has(cod)) return cod;
  }
  return LIMBA_IMPLICITA;
}

const CHEIE_STOCARE = "g-limba";

function limbaSalvata() {
  try {
    const v = localStorage.getItem(CHEIE_STOCARE);
    return CODURI.has(v) ? v : null;
  } catch {
    // Safari in mod privat, sau localStorage dezactivat: nu blocam pagina.
    return null;
  }
}

const LimbaContext = createContext(null);

export function LimbaProvider({ children }) {
  const [cod, setCod] = useState(
    () => limbaSalvata() || detecteazaLimba(navigator.languages));

  useEffect(() => {
    try { localStorage.setItem(CHEIE_STOCARE, cod); } catch { /* vezi mai sus */ }
  }, [cod]);

  const valoare = useMemo(() => {
    const limba = LIMBI.find((l) => l.cod === cod) || LIMBI[0];
    return { cod: limba.cod, steag: limba.steag, seteazaLimba: setCod };
  }, [cod]);

  return <LimbaContext.Provider value={valoare}>{children}</LimbaContext.Provider>;
}

export function useLimba() {
  const ctx = useContext(LimbaContext);
  if (!ctx) throw new Error("useLimba() cere <LimbaProvider> deasupra in arbore.");
  return ctx;
}
```

- [ ] **Step 4: Rulează testul din nou**

Run: `npx vitest run src/guest/limbi.test.js`
Expected: PASS, toate cele 6 teste.

- [ ] **Step 5: Înveleşte `<App/>` cu `<LimbaProvider>`**

```jsx
// guest/main.jsx
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import App from "../src/guest/App.jsx";
import { LimbaProvider } from "../src/guest/limbi.jsx";
import { STILURI } from "../src/guest/styles.js";
import { pregatesteManifestul } from "../src/guest/instalare.js";

const stil = document.createElement("style");
stil.textContent = STILURI;
document.head.appendChild(stil);

pregatesteManifestul();

createRoot(document.getElementById("oaspete")).render(
  <StrictMode>
    <LimbaProvider>
      <App />
    </LimbaProvider>
  </StrictMode>,
);
```

- [ ] **Step 6: Rulează suita completă și verifică vizual**

Run: `npx vitest run` — toate trec.
Run: `npm run dev:guest` — pagina se deschide identic ca înainte (Context-ul
încă n-are niciun consumator vizibil).

- [ ] **Step 7: Commit**

```bash
git add src/guest/limbi.jsx src/guest/limbi.test.js guest/main.jsx
git commit -m "Guest app: catalogul celor 7 limbi, detectare dupa telefon, Context"
```

---

### Task 3: Extrage textele de interfață din `App.jsx`/`Fisa.jsx` în `interfata.ro.js`

Cel mai întins task: fiecare șir românesc scris direct în JSX (nu în
`continut.js`) se mută într-un dicționar, cu o cheie mnemonică. Task-ul
ăsta NU traduce nimic încă — doar mută textul românesc într-un loc unde
poate fi tradus mai târziu (Task 6).

**Files:**
- Create: `src/guest/interfata.ro.js`
- Create: `src/guest/interfata.js` (dispecer, single-limbă deocamdată — ca `continut.js` din Task 1)
- Modify: `src/guest/App.jsx` (toate locurile enumerate mai jos)
- Modify: `src/guest/Fisa.jsx` (etichete de câmp, mesaje de eroare)
- Modify: `src/lib/fisa.js` (`CAMPURI`/`ACT_TIPURI` — etichetele mută în `interfata.ro.js`, `lib/fisa.js` păstrează doar `cheie`/`obligatoriu`/`sensibil`/`tip`)
- Test: teste existente pentru `lib/fisa.js` (`campuriLipsa`, `valideazaFisa`) trebuie adaptate să primească eticheta din exterior sau să continue să funcționeze pe chei — verifică `src/*.test.js` care importă din `lib/fisa.js`

**Interfaces:**
- Consumes: nimic nou din Task 1/2 (independent de ele — poate rula în paralel dacă alt executor face Task 2).
- Produces: `useTexte()` din `interfata.js`, folosit de Task 4 (dispecerul real) și Task 6 (traducerile).

- [ ] **Step 1: Scoate etichetele din `lib/fisa.js`**

`CAMPURI` și `ACT_TIPURI` de azi amestecă date de validare (`cheie`,
`obligatoriu`, `sensibil`, `tip`) cu text de-afișat (`eticheta`). Separă:

```js
// src/lib/fisa.js — CAMPURI pierde `eticheta`, pastreaza restul
export const ACT_TIPURI = [
  { cheie: "ci" },
  { cheie: "pasaport" },
  { cheie: "permis" },
];

export const CAMPURI = [
  { cheie: "nume",          obligatoriu: true,  sensibil: false },
  { cheie: "prenume",       obligatoriu: true,  sensibil: false },
  { cheie: "dataNasterii",  obligatoriu: true,  sensibil: true,  tip: "date" },
  { cheie: "loculNasterii", obligatoriu: true,  sensibil: true },
  { cheie: "nationalitate", obligatoriu: true,  sensibil: false },
  { cheie: "tara",          obligatoriu: true,  sensibil: false },
  { cheie: "adresa",        obligatoriu: true,  sensibil: false },
  { cheie: "localitate",    obligatoriu: true,  sensibil: false },
  { cheie: "scopul",        obligatoriu: true,  sensibil: false },
  { cheie: "actTip",        obligatoriu: true,  sensibil: true,  tip: "alegere" },
  { cheie: "actSeria",      obligatoriu: false, sensibil: true },
  { cheie: "actNumarul",    obligatoriu: true,  sensibil: true },
];
```

`valideazaFisa` (linia `erori[cheie] = \`${c.eticheta} lipsește.\`;`)
NU mai poate compune mesajul aici — nu mai are eticheta. Schimbă
`campuriLipsa(date)` să întoarcă doar cheile (cum face deja azi — verifică,
nu se schimbă), iar compunerea mesajului `"${eticheta} lipsește."` se
mută în `Fisa.jsx`, care ARE acces la `interfata.js`:

```js
// src/lib/fisa.js — valideazaFisa nu mai scrie text, doar chei
export function valideazaFisa(date) {
  const lipsa = campuriLipsa(date); // neschimbat
  const erori = {};
  for (const cheie of lipsa) erori[cheie] = "LIPSA"; // marcaj, nu text
  // restul functiei (data nasterii, actTip) foloseste coduri, nu text:
  // erori.dataNasterii = "DATA_INVALIDA" | "DATA_VIITOR" | "AN_SUSPECT"
  // erori.actTip = "ACT_NECUNOSCUT"
  return { erori };
}
```

- [ ] **Step 2: Rulează testele existente ale lui `lib/fisa.js`, corectează-le**

Run: `npx vitest run src/lib/fisa.test.js` (sau numele exact — caută cu
`Grep -l "valideazaFisa" --glob "*.test.js"`)
Expected: testele care verificau mesajul EXACT („Nume lipsește.") trebuie
schimbate să verifice codul („LIPSA") — schimbarea e intenționată, nu o
regresie: mesajul afișat oaspetelui se compune acum în `Fisa.jsx`, din
cod + limbă curentă.

- [ ] **Step 3: Scrie `src/guest/interfata.ro.js`**

```js
/* Textele de interfata ale paginii oaspetelui — butoane, etichete, mesaje
 * de eroare, tot ce e scris direct in JSX si NU vine din continut.js.
 * Aceeasi forma trebuie sa existe si in interfata.en.js, .fr.js, .it.js,
 * .de.js, .ru.js, .uk.js — verificat de continut-forme.test.js. */
export const TEXTE = {
  acces: {
    eticheta: "Acces cameră",
    deschideUsa: "Deschide ușa",
    sauTasteaza: "Sau tastează codul pe ușă",
    valabilPana: "Valabil până",
    codLipsa: "Codul nu e încă pregătit. Reîncarcă pagina în câteva minute sau sună-ne — între timp, poți intra cu butonul de mai sus.",
  },
  refuzuri: {
    neinceput: { titlu: "Sejurul n-a început încă", text: "Pagina se deschide singură când te cazezi. Codul de acces apare aici imediat după check-in." },
    incheiat: { titlu: "Sejurul s-a încheiat", text: "Linkul a expirat odată cu plecarea. Îți mulțumim că ai stat la noi." },
    anulat: { titlu: "Rezervarea nu mai este activă", text: "Dacă e o greșeală, sună-ne și o lămurim pe loc." },
    necunoscut: { titlu: "Linkul nu funcționează", text: "Verifică dacă l-ai deschis întreg, așa cum l-ai primit. Dacă tot nu merge, sună-ne." },
    "prea-multe": { titlu: "Prea multe încercări", text: "Așteaptă câteva minute și încearcă din nou. Dacă te grăbești, sună-ne." },
    lipsa: { titlu: "Link incomplet", text: "Adresa nu conține codul sejurului. Deschide linkul întreg, așa cum l-ai primit." },
    eroare: { titlu: "Ceva n-a mers", text: "N-am putut încărca datele. Încearcă din nou; dacă tot nu merge, sună-ne." },
    timeout: { titlu: "Serverul n-a răspuns", text: "Nu e vina linkului: serverul n-a răspuns la timp. Verifică internetul și încearcă din nou; dacă ești în fața ușii, sună-ne." },
  },
  instalare: {
    pasiIos: [
      { inainte: "Apasă ", tare: "Partajare", dupa: " — pătratul cu săgeata în sus, în bara de jos." },
      { inainte: "Derulează și alege ", tare: "Adaugă la ecranul principal", dupa: "." },
      { inainte: "Confirmă cu ", tare: "Adaugă", dupa: ", sus în dreapta." },
    ],
    pasiAndroid: [
      { inainte: "Apasă ", tare: "⋮", dupa: " în colțul din dreapta sus." },
      { inainte: "Alege ", tare: "Adaugă la ecranul principal", dupa: " sau ", tare2: "Instalează aplicația", dupa2: "." },
      { inainte: "Confirmă cu ", tare: "Adaugă", dupa: "." },
    ],
    pasiWifiIos: [
      { inainte: "Deschide ", tare: "Setări", dupa: " → ", tare2: "Wi-Fi", dupa2: "." },
      { inainte: "Alege ", dupa: " din listă." }, // `tare` = WIFI.retea, injectat in App.jsx
      { text: "Gata — rețeaua nu cere parolă." },
    ],
    pasiWifiAndroid: [
      { inainte: "Trage în jos bara de sus și ține apăsat pe ", tare: "Wi-Fi", dupa: "." },
      { inainte: "Alege ", dupa: " din listă." },
      { text: "Gata — rețeaua nu cere parolă." },
    ],
  },
  fisa: {
    titluFereastra: "Fișă de cazare",
    intro: "E obligatorie la cazare, o singură dată pe sejur.",
    ajutor: { inainte: "Rețeaua ", mijloc: ", fără parolă. Dacă te împotmolești, sună-l pe ", dupa: " la" },
    campEtichete: {
      nume: "Nume", prenume: "Prenume", dataNasterii: "Data nașterii",
      loculNasterii: "Locul nașterii", nationalitate: "Naționalitate",
      tara: "Țara de domiciliu", adresa: "Adresa", localitate: "Localitatea",
      scopul: "Scopul călătoriei", actTip: "Act de identitate",
      actSeria: "Seria", actNumarul: "Numărul",
    },
    actTipEtichete: { ci: "Carte de identitate", pasaport: "Pașaport", permis: "Permis de ședere" },
    dacaAre: "(dacă are)",
    dataZiua: "Ziua", dataLuna: "Luna", dataAnul: "Anul",
    eroriCamp: {
      LIPSA: (eticheta) => `${eticheta} lipsește.`,
      DATA_INVALIDA: "Data nașterii nu e o dată validă.",
      DATA_VIITOR: "Data nașterii nu poate fi în viitor.",
      AN_SUSPECT: "Verifică anul nașterii.",
      ACT_NECUNOSCUT: "Alege un tip de act din listă.",
    },
    eroareSemnatura: "Semnează în chenarul de mai sus.",
    dejaCompletata: "Fișa e deja completată.",
    eroareTrimitere: "Nu am putut trimite fișa. Mai încearcă o dată.",
    trimite: "Semnez și trimit",
    trimitAcum: "Trimit…",
    seIncarca: "Se încarcă…",
  },
};
```

- [ ] **Step 4: Scrie dispecerul temporar `src/guest/interfata.js`**

```js
export { TEXTE } from "./interfata.ro.js";
export function useTexte() {
  // Single-limba deocamdata — Task 4 il face sa aleaga dupa useLimba().cod.
  return TEXTE;
}
```

- [ ] **Step 5: Actualizează `App.jsx` să citească din `useTexte()`**

Fiecare din locurile de mai jos (găsite cu `Grep` pe `src/guest/App.jsx`
din 19 septembrie 2026) își înlocuiește șirul literal cu cheia
corespunzătoare din `texte`:

| Loc (linia de azi) | Text vechi | Cheie nouă |
|---|---|---|
| 71-106 `REFUZURI` | obiectul întreg | `texte.refuzuri` |
| 1017 | `"Acces cameră"` | `texte.acces.eticheta` |
| 727 | `"Deschide ușa"` | `texte.acces.deschideUsa` |
| 1044 | `"Sau tastează codul pe ușă"` | `texte.acces.sauTasteaza` |
| 1049 | `"Valabil până"` | `texte.acces.valabilPana` |
| 1054 | mesajul „Codul nu e încă pregătit..." | `texte.acces.codLipsa` |
| 256-276 `PASI_IOS`/`PASI_ANDROID`/`PASI_WIFI_IOS`/`PASI_WIFI_ANDROID` | cele 4 array-uri | `texte.instalare.pasiIos` etc., randate cu o funcție `randPas(pas)` nouă (vezi Step 6) |

```jsx
import { useTexte } from "./interfata.js";
// ...
const texte = useTexte();
const REFUZURI = texte.refuzuri; // inlocuieste constanta modul-level
```

- [ ] **Step 6: Randează pașii de instalare din date, nu din JSX fix**

Array-urile `PASI_*` erau JSX gata format (`<>Apasă <b>Partajare</b>...</>`).
Acum sunt date (`{ inainte, tare, dupa, tare2?, dupa2? }` sau `{ text }`).
Scrie o funcție mică de randare, lângă unde erau folosite pașii:

```jsx
function Pas({ p }) {
  if (p.text) return <>{p.text}</>;
  return <>{p.inainte}<b>{p.tare}</b>{p.dupa}{p.tare2 && <><b>{p.tare2}</b>{p.dupa2}</>}</>;
}
```

Pentru `pasiWifiIos`/`pasiWifiAndroid`, al doilea pas n-are `tare` scris —
se injectează `WIFI.retea` la randare: `<Pas p={{ ...pas, tare: WIFI.retea }} />`
pentru pasul cu indexul 1 din fiecare array.

- [ ] **Step 7: Actualizează `Fisa.jsx`**

- Titlul ferestrei: `<Fereastra titlu={texte.fisa.titluFereastra} ...>`.
- Intro: `texte.fisa.intro`.
- Ajutor Wi-Fi: recompune paragraful cu `texte.fisa.ajutor.inainte`,
  `{WIFI.retea}`, `texte.fisa.ajutor.mijloc`, `{ASISTENTA.nume}`,
  `texte.fisa.ajutor.dupa`, link-ul `tel:`.
- Etichetele câmpurilor: `texte.fisa.campEtichete[c.cheie]` în loc de
  `c.eticheta`.
- Tipurile de act: `texte.fisa.actTipEtichete[t.cheie]` în loc de
  `t.eticheta`.
- `(dacă are)`: `texte.fisa.dacaAre`.
- Etichetele zi/lună/an din `DataNasterii`: `texte.fisa.dataZiua` etc.
- Mesajele de eroare: `valideazaFisa` întoarce coduri acum (Step 1-2) —
  `Fisa.jsx` le traduce cu `texte.fisa.eroriCamp[cod]`, iar pentru
  `LIPSA` (care are nevoie de eticheta câmpului) apelează funcția:
  `texte.fisa.eroriCamp.LIPSA(texte.fisa.campEtichete[cheie])`.
- `esteGoala(linii)` → mesajul de semnătură: `texte.fisa.eroareSemnatura`.
- Răspunsul serverului: `texte.fisa.dejaCompletata` / `texte.fisa.eroareTrimitere`.
- Butonul: `texte.fisa.trimite` / `texte.fisa.trimitAcum`.
- „Se încarcă…": `texte.fisa.seIncarca` (folosit și de fișă, și de
  `Fereastra`-urile de acces/regulament dacă afișează starea de
  încărcare cu același text).

- [ ] **Step 8: Rulează suita completă**

Run: `npx vitest run`
Expected: toate trec (inclusiv testele corectate de la Step 2).

- [ ] **Step 9: Verifică vizual în browser**

Deschide fișa de cazare, lasă câmpuri goale, apasă trimite — mesajele de
eroare trebuie să arate identic cu înainte. Deschide panoul de instalare
pe simulare iOS și Android (User-Agent din devtools) — pașii trebuie să
arate identic, cu „Wi-Fi"/numele rețelei încă îngroșate corect.

- [ ] **Step 10: Commit**

```bash
git add src/guest/interfata.ro.js src/guest/interfata.js src/guest/App.jsx src/guest/Fisa.jsx src/lib/fisa.js
git commit -m "Guest app: textele de interfata mutate in interfata.ro.js, pregatite de tradus"
```

---

### Task 4: Testul de formă comună + dispecerele reale (`continut.js`, `interfata.js` aleg după `useLimba()`)

**Files:**
- Create: `src/guest/continut.en.js`, `.fr.js`, `.it.js`, `.de.js`, `.ru.js`, `.uk.js` (temporar: `export * from "./continut.ro.js"`)
- Create: `src/guest/interfata.en.js`, `.fr.js`, `.it.js`, `.de.js`, `.ru.js`, `.uk.js` (idem)
- Create: `src/guest/continut-forme.test.js`
- Modify: `src/guest/continut.js` (dispecer real, static import al celor 7)
- Modify: `src/guest/interfata.js` (dispecer real, static import al celor 7)

**Interfaces:**
- Consumes: `useLimba()` din Task 2, `interfata.ro.js`/`continut.ro.js` din Task 1/3.
- Produces: `useContinutMic()` (nou, înlocuiește re-exportul static de
  `BUN_VENIT`/`IMPORTANT` din `continut.js`), `useTexte()` (actualizat,
  reactiv la limbă).

- [ ] **Step 1: Scrie testul de formă comună**

```js
// src/guest/continut-forme.test.js
import { describe, it, expect } from "vitest";
import * as ro from "./continut.ro.js";
import * as en from "./continut.en.js";
import * as fr from "./continut.fr.js";
import * as it from "./continut.it.js";
import * as de from "./continut.de.js";
import * as ru from "./continut.ru.js";
import * as uk from "./continut.uk.js";
import * as interfataRo from "./interfata.ro.js";
import * as interfataEn from "./interfata.en.js";
import * as interfataFr from "./interfata.fr.js";
import * as interfataIt from "./interfata.it.js";
import * as interfataDe from "./interfata.de.js";
import * as interfataRu from "./interfata.ru.js";
import * as interfataUk from "./interfata.uk.js";

/* Compara forma (cheile, recursiv), nu continutul — o traducere e altfel
 * ca text, dar trebuie sa aiba EXACT aceleasi chei ca sursa romana. Un
 * camp uitat la o traducere ar lasa oaspetele cu un "undefined" pe ecran,
 * neobservat de niciun test care doar verifica ca fisierul exista. */
function chei(obiect, prefix = "") {
  if (Array.isArray(obiect)) {
    return obiect.length > 0 ? chei(obiect[0], `${prefix}[]`) : [`${prefix}[]`];
  }
  if (obiect && typeof obiect === "object") {
    return Object.keys(obiect).sort()
      .flatMap((k) => chei(obiect[k], prefix ? `${prefix}.${k}` : k));
  }
  return [prefix];
}

describe("forma comuna a continutului pe limbi", () => {
  const modulePeLimba = { en, fr, it, de, ru, uk };
  const cheieSursa = chei(ro);

  for (const [cod, modul] of Object.entries(modulePeLimba)) {
    it(`continut.${cod}.js are exact cheile din continut.ro.js`, () => {
      expect(chei(modul)).toEqual(cheieSursa);
    });
  }

  const cheieInterfataSursa = chei(interfataRo);
  const interfataPeLimba = {
    en: interfataEn, fr: interfataFr, it: interfataIt,
    de: interfataDe, ru: interfataRu, uk: interfataUk,
  };
  for (const [cod, modul] of Object.entries(interfataPeLimba)) {
    it(`interfata.${cod}.js are exact cheile din interfata.ro.js`, () => {
      expect(chei(modul)).toEqual(cheieInterfataSursa);
    });
  }
});
```

- [ ] **Step 2: Creează cele 6 fișiere-punte pentru fiecare dispecer**

```js
// src/guest/continut.en.js (si identic pentru fr/it/de/ru/uk, schimband doar numele fisierului)
/* PUNTE TEMPORARA — inlocuit cu traduceri reale in Task 6.
 * Pana atunci, oaspetele care alege engleza vede text romanesc: asta e
 * de asteptat si dispare cand Task 6 inlocuieste continutul de-a dreptul,
 * fara sa schimbe forma testata de continut-forme.test.js. */
export * from "./continut.ro.js";
```

(identic, cu comentariul adaptat, pentru `interfata.en.js` etc.)

- [ ] **Step 3: Rulează testul de formă**

Run: `npx vitest run src/guest/continut-forme.test.js`
Expected: PASS — toate „traducerile" sunt încă re-exporturi identice, deci
cheile se potrivesc trivial. Testul ăsta devine util abia după Task 6,
dar trebuie să treacă și acum, ca infrastructura să fie verificată
înaintea traducerilor reale.

- [ ] **Step 4: Fă din `continut.js` dispecerul real**

```js
// src/guest/continut.js
import { useLimba } from "./limbi.jsx";
export * from "./date.js"; // faptele, neschimbate de limba

import * as ro from "./continut.ro.js";
import * as en from "./continut.en.js";
import * as fr from "./continut.fr.js";
import * as it from "./continut.it.js";
import * as de from "./continut.de.js";
import * as ru from "./continut.ru.js";
import * as uk from "./continut.uk.js";

const MODULE = { ro, en, fr, it, de, ru, uk };

export function useContinutMic() {
  const { cod } = useLimba();
  const m = MODULE[cod];
  return { BUN_VENIT: m.BUN_VENIT, IMPORTANT: m.IMPORTANT };
}

export function useAsistentaRaspuns() {
  const { cod } = useLimba();
  return MODULE[cod].asistentaRaspuns;
}

export function useAccesCamereDescrieri() {
  const { cod } = useLimba();
  return MODULE[cod].ACCES_CAMERE_DESCRIERI;
}

export function useAtractiiText() {
  const { cod } = useLimba();
  return MODULE[cod].ATRACTII_TEXT;
}
```

(Regulamentul rămâne EXPLICIT în afara acestui fișier — se mută în
`continut-mare.js` la Task 7, cu `import()` dinamic, nu static ca mai
sus.)

- [ ] **Step 5: Fă din `interfata.js` dispecerul real**

```js
// src/guest/interfata.js
import { useLimba } from "./limbi.jsx";
import * as ro from "./interfata.ro.js";
import * as en from "./interfata.en.js";
import * as fr from "./interfata.fr.js";
import * as it from "./interfata.it.js";
import * as de from "./interfata.de.js";
import * as ru from "./interfata.ru.js";
import * as uk from "./interfata.uk.js";

const MODULE = { ro, en, fr, it, de, ru, uk };

export function useTexte() {
  const { cod } = useLimba();
  return MODULE[cod].TEXTE;
}
```

- [ ] **Step 6: Actualizează `App.jsx`/`Fisa.jsx` să folosească hook-urile noi**

`BUN_VENIT`/`IMPORTANT` nu mai sunt import static — devin
`const { BUN_VENIT, IMPORTANT } = useContinutMic();` în componenta care le
folosește. Similar pentru `asistentaRaspuns` → `useAsistentaRaspuns()`,
descrierile de acces → `useAccesCamereDescrieri()`, textul atracțiilor →
`useAtractiiText()`.

- [ ] **Step 7: Rulează suita completă și verifică vizual**

Run: `npx vitest run` — toate trec.
Run: `npm run dev:guest` — pagina arată identic (toate limbile dau azi
același text românesc, din re-exporturi).

- [ ] **Step 8: Commit**

```bash
git add src/guest/continut*.js src/guest/interfata*.js src/guest/App.jsx src/guest/Fisa.jsx
git commit -m "Guest app: dispecerele de limba, cu test de forma comuna intre cele 7 fisiere"
```

---

### Task 5: Selectorul de limbă (buton + fereastră)

**Files:**
- Modify: `src/guest/App.jsx` (butonul, lista celor 7 rânduri, plasarea)
- Modify: `src/guest/styles.js` (CSS-ul butonului și al rândurilor din fereastră)

**Interfaces:**
- Consumes: `useLimba()` (Task 2), `Fereastra` (existent, neschimbat).
- Produces: nimic consumat de alte task-uri — ultima piesă vizibilă.

- [ ] **Step 1: Scrie componenta `SelectorLimba`, lângă `Vremea`**

```jsx
// App.jsx, langa functia Vremea()
import { LIMBI, useLimba } from "./limbi.jsx";

function SelectorLimba() {
  const { cod, steag, seteazaLimba } = useLimba();
  const [deschis, setDeschis] = useState(false);

  return (
    <>
      <button type="button" className="g-limba-buton" onClick={() => setDeschis(true)}
        aria-label="Alege limba paginii">
        <span aria-hidden="true">{steag}</span>
      </button>
      {deschis && (
        <Fereastra titlu="Alege limba" onInchide={() => setDeschis(false)}>
          <div className="g-limba-lista">
            {LIMBI.map((l) => (
              <button key={l.cod} type="button" className="g-limba-rand"
                aria-current={l.cod === cod}
                onClick={() => { seteazaLimba(l.cod); setDeschis(false); }}>
                <span aria-hidden="true">{l.steag}</span>
                <span>{l.nume}</span>
              </button>
            ))}
          </div>
        </Fereastra>
      )}
    </>
  );
}
```

- [ ] **Step 2: Pune-o înaintea vremii, pe rândul de sus**

```jsx
// in randul .g-salut-sus, unde azi sta doar <Vremea />
<div className="g-salut-sus">
  <p className="g-salut-ora">{salut(new Date().getHours())}</p>
  <SelectorLimba />
  <Vremea />
</div>
```

- [ ] **Step 3: Adaugă CSS-ul în `styles.js`**

```css
/* ---------- selectorul de limba ---------- */
.g-limba-buton{
  border:0; background:none; padding:4px; margin-right:8px;
  font-size:20px; line-height:1; cursor:pointer; touch-action:manipulation;
}
.g-limba-lista{ display:flex; flex-direction:column; gap:2px; }
.g-limba-rand{
  display:flex; align-items:center; gap:10px; width:100%;
  background:none; border:0; border-radius:10px; padding:11px 10px;
  cursor:pointer; font:inherit; font-size:15px; color:var(--g-text);
  text-align:left; touch-action:manipulation;
}
.g-limba-rand[aria-current="true"]{ background:var(--beige); font-weight:600; }
.g-limba-rand span:first-child{ font-size:20px; flex-shrink:0; }
```

- [ ] **Step 4: Verifică în browser**

`npm run dev:guest`: steagul apare înaintea vremii; click deschide
fereastra (Escape/clic-pe-fundal/Tab-trap deja funcționează, moștenite
de la `Fereastra.jsx`); alegerea unei limbi o marchează cu
`aria-current` la următoarea deschidere și schimbă (dacă Task 6 a livrat
deja traducerile) textul paginii.

- [ ] **Step 5: Rulează suita completă**

Run: `npx vitest run` — toate trec. Adaugă un test de stiluri dacă
`guest-stiluri.test.js` verifică prezența claselor CSS noi (după modelul
existent din fișierul ăla).

- [ ] **Step 6: Commit**

```bash
git add src/guest/App.jsx src/guest/styles.js
git commit -m "Guest app: selector de limba (steag + fereastra cu cele 7 limbi)"
```

---

### Task 6: Traducerile stratului mic (EN/FR/IT/DE/RU/UK) — `interfata.<lang>.js` + `continut.<lang>.js` (BUN_VENIT, IMPORTANT)

**Files:**
- Modify: `src/guest/interfata.en.js`, `.fr.js`, `.it.js`, `.de.js`, `.ru.js`, `.uk.js` (înlocuiesc `export * from "./interfata.ro.js"` cu traduceri reale)
- Modify: `src/guest/continut.en.js`, `.fr.js`, `.it.js`, `.de.js`, `.ru.js`, `.uk.js` (idem, doar `BUN_VENIT`/`IMPORTANT`/`asistentaRaspuns`/`ACCES_CAMERE_DESCRIERI` — `REGULAMENT` ȘI `ATRACTII_TEXT` rămân re-export până la Task 7, care le mută pe amândouă în stratul mare, per §2 din spec)

**Proces (obligatoriu, per §0 din spec — nu se sare peste):**
1. Executorul redactează traducerea completă pentru O limbă (toate cheile
   din `interfata.ro.js` + partea mică din `continut.ro.js`).
2. O arată proprietarului într-un mesaj din chat, text simplu (nu commit),
   limbă cu limbă — nu toate 6 deodată.
3. Așteaptă aprobarea sau corecturile.
4. Abia după aprobare, scrie fișierul și trece la Step-urile de mai jos
   pentru limba aia.
5. Repetă pentru următoarea limbă.

**Interfaces:**
- Consumes: `continut-forme.test.js` (Task 4) verifică fiecare fișier nou.
- Produces: nimic nou consumat de alte task-uri.

Pentru FIECARE din cele 6 limbi:

- [ ] **Step 1: Redactează traducerea și arat-o proprietarului**

Fără cod încă — un mesaj din chat cu textul propus, structurat pe
secțiuni (Acces, Refuzuri, Instalare, Fișă, Bun venit, Important),
identificând clar limba.

- [ ] **Step 2: După aprobare, scrie fișierul `interfata.<lang>.js`**

Aceeași formă exactă ca `interfata.ro.js` (Task 3, Step 3) — toate
cheile, traduse.

- [ ] **Step 3: Scrie `continut.<lang>.js`**

```js
// exemplu de forma (engleza) — continutul real vine din traducerea aprobata
export const asistentaRaspuns = "replies within a few minutes";

export const BUN_VENIT = {
  intro: "We're glad you're here and wish you a pleasant stay.",
  puncte: [
    { titlu: "Free Wi-Fi", inainte: "The ", dupa: " network, no password.", actiune: "wifi" },
    { titlu: "Save this page to your phone", text: "Your code and unlock button stay one tap away for your whole stay.", actiune: "instalare" },
  ],
};

export const IMPORTANT = [
  { titlu: "Smoking", text: "Not allowed inside rooms or indoor areas — outside is fine. Smoking in a room is charged 500 RON." },
  { titlu: "Quiet hours", text: "Please keep quiet between 22:00 and 08:00." },
  { titlu: "Pets", text: "Welcome, for a 50 RON fee per stay." },
  { titlu: "Parking", text: "Free on site, at the car owner's own risk." },
];

export const ACCES_CAMERE_DESCRIERI = [
  { fisier: "1-intrarea.jpg", descriere: "Enter through the parking area on the right and follow the road." },
  { fisier: "2-aleea.jpg",    descriere: "At this sign, keep going straight ahead." },
  { fisier: "3-parcarea.jpg", descriere: "You've reached the parking lot." },
  { fisier: "4-poteca.jpg",   descriere: "Rooms are accessed via the path on the right, past the Grand'Or Ballroom." },
];

export { REGULAMENT, ATRACTII_TEXT } from "./continut.ro.js"; // inca netraduse — Task 7 (stratul mare, §2 din spec)
```

- [ ] **Step 4: Rulează testul de formă și suita completă**

Run: `npx vitest run`
Expected: PASS pentru limba nou tradusă (celelalte, încă pe re-export,
trec și ele — forma nu s-a schimbat).

- [ ] **Step 5: Verifică vizual în browser, în limba respectivă**

`npm run dev:guest`, alege limba din selector, verifică fiecare ecran
(hero, fișă, cele patru scurtături, mesajele de refuz, pașii de
instalare) — REGULAMENT și ATRACTII rămân în română până la Task 7, e de
așteptat.

- [ ] **Step 6: Commit (o singură limbă per commit)**

```bash
git add src/guest/interfata.en.js src/guest/continut.en.js
git commit -m "Guest app: traducere in engleza (interfata + continut mic)"
```

Repetă Step 1-6 pentru fr, it, de, ru, uk — șase commit-uri separate,
fiecare cu propria aprobare din chat înainte.

---

### Task 7: Regulamentul și Atracțiile — încărcare leneșă per limbă (`continut-mare.js`) + traducerile reale

**Files:**
- Create: `src/guest/continut-mare.js`
- Modify: `src/guest/continut.<lang>.js` (toate 7 — `REGULAMENT` iese complet din `continut.<lang>.js`, se mută în module separate, mai jos)
- Create: `src/guest/regulament.ro.js`, `.en.js`, `.fr.js`, `.it.js`, `.de.js`, `.ru.js`, `.uk.js` (REGULAMENT, un modul per limbă, ca să fie `import()`-abil separat)
- Modify: `src/guest/App.jsx` (panoul de regulament și cel de atracții, cu stare de încărcare)

**Interfaces:**
- Consumes: `useLimba()` (Task 2).
- Produces: `useRegulament()` → `{ stare: "incarca" | "gata", regulament }`;
  `useAtractiiTextMare()` → la fel, pentru `ATRACTII_TEXT` (mutat din
  `continut.<lang>.js` aici, din același motiv — text lung, citit rar).

De ce separat de `continut.<lang>.js`: dacă `REGULAMENT` ar rămâne în
`continut.en.js` alături de `BUN_VENIT`/`IMPORTANT` (Task 6), un
`import()` dinamic al fișierului ar aduce ȘI conținutul mic o dată în
plus — exact dubla încărcare pe care Task-ul 2 din spec voia s-o evite.
Separarea în fișiere proprii (`regulament.<lang>.js`) face ca `import()`-ul
să aducă DOAR regulamentul.

- [ ] **Step 1: Mută `REGULAMENT` din `continut.ro.js` în `regulament.ro.js`**

```js
// src/guest/regulament.ro.js
export const REGULAMENT = {
  intro: "Oaspeții au luat la cunoștință următoarele reguli și se obligă să le respecte:",
  reguli: [
    // cele 25 de linii, mutate EXACT din continut.ro.js — nu retranscrise.
  ],
};
```

Elimină `REGULAMENT` din `continut.ro.js` (și din re-exportul
`export { REGULAMENT } from "./continut.ro.js";` din fiecare
`continut.<lang>.js` scris la Task 6).

- [ ] **Step 2: Mută `ATRACTII_TEXT` similar, într-un modul propriu**

```js
// src/guest/atractii-text.ro.js
export const ATRACTII_TEXT = [
  // cele 10 intrari, mutate din continut.ro.js
];
```

Elimină și hook-ul `useAtractiiText()` din `continut.js` (scris la
Task 4) — după acest pas, `MODULE[cod].ATRACTII_TEXT` nu mai există în
`continut.<lang>.js`, deci hook-ul ar întoarce `undefined` dacă rămâne.
Singurul export valabil de-acum pentru textul atracțiilor e
`useAtractiiTextMare()` din `continut-mare.js` (Step 3 mai jos); orice
consumator al vechiului `useAtractiiText()` trece pe acesta (Step 4).

- [ ] **Step 3: Scrie `continut-mare.js`, cu `import()` dinamic**

```js
// src/guest/continut-mare.js
import { useEffect, useState } from "react";
import { useLimba } from "./limbi.jsx";

const INCARCATOR_REGULAMENT = {
  ro: () => import("./regulament.ro.js"),
  en: () => import("./regulament.en.js"),
  fr: () => import("./regulament.fr.js"),
  it: () => import("./regulament.it.js"),
  de: () => import("./regulament.de.js"),
  ru: () => import("./regulament.ru.js"),
  uk: () => import("./regulament.uk.js"),
};

const INCARCATOR_ATRACTII = {
  ro: () => import("./atractii-text.ro.js"),
  en: () => import("./atractii-text.en.js"),
  fr: () => import("./atractii-text.fr.js"),
  it: () => import("./atractii-text.it.js"),
  de: () => import("./atractii-text.de.js"),
  ru: () => import("./atractii-text.ru.js"),
  uk: () => import("./atractii-text.uk.js"),
};

/* Fabrica comuna: acelasi tipar de incarcare leneasa pentru regulament si
 * atractii, doar sursa difera. Recalculeaza la schimbarea limbii — un
 * oaspete care schimba limba cu fereastra regulamentului deschisa vede
 * noul text, nu ramane cu cel vechi in cache. */
function useContinutMare(incarcator, extrage) {
  const { cod } = useLimba();
  const [stare, setStare] = useState({ stare: "incarca", date: null });

  useEffect(() => {
    let viu = true;
    setStare({ stare: "incarca", date: null });
    incarcator[cod]()
      .then((modul) => { if (viu) setStare({ stare: "gata", date: extrage(modul) }); });
    return () => { viu = false; };
  }, [cod]);

  return stare;
}

export function useRegulament() {
  const { stare, date } = useContinutMare(INCARCATOR_REGULAMENT, (m) => m.REGULAMENT);
  return { stare, regulament: date };
}

export function useAtractiiTextMare() {
  const { stare, date } = useContinutMare(INCARCATOR_ATRACTII, (m) => m.ATRACTII_TEXT);
  return { stare, texte: date };
}
```

- [ ] **Step 4: Actualizează panoul de regulament în `App.jsx`**

```jsx
function PanouRegulament() {
  const { stare, regulament } = useRegulament();
  const texte = useTexte();
  if (stare === "incarca") return <p className="g-gol">{texte.fisa.seIncarca}</p>;
  return (
    <>
      <p>{regulament.intro}</p>
      <ol>{regulament.reguli.map((r, i) => <li key={i}>{r}</li>)}</ol>
    </>
  );
}
```

Actualizează similar panoul de atracții, combinând `useAtractiiTextMare()`
(text) cu `ATRACTII` din `date.js` (fapte), prin `cheie` — la fel ca la
Task 1, Step 4, dar acum cu o stare de încărcare de gestionat.

- [ ] **Step 5: Actualizează testul de formă**

`continut-forme.test.js` (Task 4) capătă o secțiune nouă: compară cheile
`regulament.<lang>.js` și `atractii-text.<lang>.js` față de sursele
românești, la fel ca restul.

- [ ] **Step 6: Traduce Regulamentul și Atracțiile, o limbă pe rând**

Același proces de aprobare ca Task 6 — arată textul, așteaptă
aprobarea, abia apoi scrie fișierul. Regulamentul e text juridic (vezi
§7 din spec: „valoarea lui legală ar trebui confirmată separat, la
nevoie, de cineva cu pregătire juridică"); marchează asta explicit când
arăți traducerea, nu doar o livrezi ca pe restul textului.

- [ ] **Step 7: Rulează suita completă și verifică vizual**

Run: `npx vitest run` — toate trec, inclusiv verificările de formă
pentru regulament/atracții pe toate limbile.
Run: `npm run dev:guest` — deschide regulamentul și atracțiile în
fiecare din cele 7 limbi.

- [ ] **Step 8: Commit (per limbă, ca la Task 6)**

```bash
git add src/guest/regulament.en.js src/guest/atractii-text.en.js
git commit -m "Guest app: regulament si atractii traduse in engleza"
```

---

### Task 8: Verificare finală — bundle, cele 7 limbi, code-splitting

**Files:** niciunul modificat — task de verificare pură.

- [ ] **Step 1: Măsoară bundle-urile**

Run: `npm run build:guest`
Expected: `dist-guest/assets/` conține fișiere JS separate per limbă
pentru regulament/atracții (numele conțin un hash, dar ar trebui să vezi
7 bucăți mici distincte, nu una singură cu tot conținutul).

- [ ] **Step 2: Verifică fiecare limbă în browser, pe rând**

Pentru fiecare din cele 7 coduri: setează `navigator.languages` (sau
`localStorage.setItem("g-limba", cod)` + reîncarcă), parcurge fiecare
ecran (hero, fișă, cele patru scurtături, „Cum ajungi la noi", acces,
regulament, atracții, ecranele de refuz, pașii de instalare) și confirmă
că nu apare niciun text românesc rămas din greșeală și niciun
`undefined`.

- [ ] **Step 3: Verifică detectarea automată**

Cu DevTools (Network conditions / Sensors, sau `navigator.languages` prin
`Object.defineProperty` într-un `javascript_exec`), simulează un telefon
setat pe franceză, apoi pe o limbă nesuportată (ex. spaniolă) — al doilea
caz trebuie să pornească în engleză, nu în română.

- [ ] **Step 4: Verifică persistarea**

Alege manual o limbă din selector, reîncarcă pagina (fără să schimbi
`navigator.languages`) — trebuie să rămână pe limba aleasă, nu pe cea
detectată.

- [ ] **Step 5: Rulează suita completă, o ultimă dată**

Run: `npx vitest run`
Expected: toate testele trec.

- [ ] **Step 6: Commit final (dacă a rezultat vreo corecție)**

```bash
git add -A
git commit -m "Guest app: verificare finala pe cele 7 limbi"
```

---

## Self-Review (făcut la scrierea planului)

**Acoperirea spec-ului:** §1 (sursa de adevăr) → Task 1+3; §2 (bundle în
două straturi) → Task 7; §3 (detectare+persistare) → Task 2; §4
(selector) → Task 5; §5 (textul din componente) → Task 3; §6 (ce nu se
traduce) → respectat explicit în `date.js` (Task 1) și în Global
Constraints; §7 (riscuri) → notat explicit în Task 6/7 (proces de
aprobare, avertismentul legal pentru regulament); §8 (ordinea livrării)
→ urmată task cu task.

**Scanare placeholder-e:** niciun „TBD"/„similar cu mai sus" în pașii de
cod — traducerile efective (Task 6/7) sunt tratate ca proces explicit de
aprobare, nu ca text de umplutură.

**Consistența tipurilor:** `useContinutMic()`, `useTexte()`,
`useRegulament()`, `useAtractiiTextMare()`, `useLimba()` au aceeași
semnătură peste tot unde sunt menționate (Task 2 le definește, Task
3-7 le consumă fără să le redefinească).
